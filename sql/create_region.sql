DROP TABLE REGION;

CREATE TABLE REGION
(REGION_ID NUMBER(2,0) PRIMARY KEY,
NAME VARCHAR2(25),
SEASON VARCHAR2(25),
TEMP_MIN NUMBER(2,0),
TEMP_MAX NUMBER (2,0),
SHAPE SDO_GEOMETRY);

GRANT SELECT,UPDATE,INSERT,DELETE ON REGION TO s1217815, s1676540;
